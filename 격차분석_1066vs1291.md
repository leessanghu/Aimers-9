# 왜 상위권이 안 됐는가 — 1066 vs 1291 정량 분석

## 1. 격차의 실제 크기

2024 폴드 기준 r=0.4861, BSref=0.249807.

| | 점수 | BSS | Brier | 예측SD | corr(p,y) |
|---|---|---|---|---|---|
| 우리(v47) | 1066.2 | 1.0662% | 0.247144 | 0.0516 | 0.1033 |
| 100등컷 | 1088 | 1.0880% | 0.247089 | 0.0521 | 0.1043 |
| 1등 | 1291 | 1.2910% | 0.246582 | 0.0568 | 0.1136 |

**격차 = BSS 0.225%p = Brier 0.000562.** 절대값은 미미하지만 상대적으로는
예측-실제 상관을 0.1033 → 0.1136으로 **10% 끌어올려야** 한다.

## 2. 이 격차를 "새 피처 하나"로 메우려면?

필요 부분상관 = sqrt(0.00225) = **0.0474**
- 우리 스크리너 점수로 환산하면 **225점**
- n=253,507에서 1시그마=0.00199이므로 **24시그마 사건**
- 우리가 72개 후보를 3단계 깔때기로 거른 결과 최대치는 **splithalf +24점(부분상관 0.0155)**

즉 필요한 신호는 우리가 찾은 최고치의 **9배**, 검출 임계값의 **25배**다.
그런 게 존재했다면 1단계 스크리너에서 즉시 튀어나왔을 것 — 즉
**"우리가 아직 못 찾은 테이블 피처"로는 이 격차가 설명되지 않는다.**

## 3. 이번 세션에서 숫자로 배제한 것들

| 가설 | 검증 | 결과 |
|---|---|---|
| 새 파생피처에 신호가 남아있다 | 72후보 3단계 깔때기(partial→splithalf→3폴드) | 생존 0개 |
| 앙상블 가중치가 최적이 아니다 | Bates-Granger 닫힌형해(QP) vs 그리드 | 거의 동일, 이미 포화 |
| 확률보정이 안 돼있다 | Platt/Isotonic/Beta (idea22) | 전부 raw 대비 마이너스 |
| 예측이 과소분산(under-confident)이다 | E[y\|p]SD/예측SD = 1.038(A), 0.991(C) | 거의 1, 샤프닝 최적 a=1.0, 이득 +0.06 |
| OOF161이 희석돼서 죽은 것이다 | base3 전체를 교체해봄 | 오히려 -4.49(A)/-31.64(C), 다양성 손실. 진짜 기각 |

## 4. 유일하게 "볼 수 없는" 곳 — 2025 레짐

로컬 `data/test.csv`는 **5행짜리 샘플**뿐(2025, game_type=R). 실제 2025 분포를
로컬에서 진단할 방법이 전혀 없다.

시즌별 양성률은 **단조 하락 중**:
```
2019 0.5647 → 2020 0.5327 → 2021 0.5328 → 2022 0.5289 → 2023 0.5000 → 2024 0.4861
```
F리그는 더 극적: 0.7087(2022) → 0.4729(2023) → 0.4593(2024)

그리고 fold A(train≤2023 → 2024, 실제 과제와 같은 1시즌-앞 구조)에서
우리 예측 평균은 **실제보다 +0.0065 높았다**(과대예측).

**핵심 산술**: 평균 예측이 체계적으로 **2.37%p**만 어긋나도 정확히 225점을 잃는다.
```
A(보정오차)  0.005 → 10점   0.010 → 40점   0.015 → 90점
             0.020 → 160점  0.024 → 231점 (= 1등과의 전체 격차)
```
2019→2024 평균 하락폭이 시즌당 약 1.5%p인 걸 감안하면, 2025에서 1.5~2.5%p
수준의 레벨 어긋남은 **충분히 현실적인 범위**다.

## 5. 리더보드 프로브로 이걸 측정할 수 있다

제출 예측 p에 상수 δ를 더하면:
```
score(δ) - score(0) = -1e5 * (2δ·A + δ²) / B      (A = E[p] - r,  B = r(1-r))
```
- δ 다른 값 2번 제출 → A와 B를 **정확히** 풀 수 있음(r도 역산됨)
- 1번만 써도 B≈0.2498 가정하면 A를 충분한 정밀도로 추정 가능
- 최적 이동 δ* = -A, 그때 이득 = 1e5·A²/B 점

**추천**: δ = -0.01 (전 예측 1%p 하향)로 1회 프로브.
- 점수 오르면 → 과대예측 확정, 최적 δ 계산해서 적용
- 점수 내리면 → 반대 방향, 역시 A를 정량적으로 알게 됨

리스크: 제출 1~2회 소모. public/private 분할이 있으면 public에 과적합될 수
있으니, 측정된 A가 작으면(<0.005) 무시하고 넘어가는 게 안전.

## 6. 결론

30개 실험으로 확인된 것은 **"모델링 여지는 거의 소진됐다"**이다. 피처·가중치·
보정·아키텍처 모든 축에서 남은 이득이 노이즈 수준으로 수렴했다.

남은 225점 격차의 가장 유력한 설명은 **2025 레짐 레벨을 우리가 못 맞추고 있다**는
것이고, 이건 로컬에서 원리적으로 진단 불가능한 유일한 축이다. 그리고 리더보드
프로브라는 **정확한 측정 수단이 존재**한다.

다음 행동 우선순위:
1. **레벨 프로브 1~2회** (비용 최소, 기대이득 0~230점, 유일하게 미탐색)
2. 프로브 결과로 A가 크면 → 전 제출에 δ* 적용
3. A가 작으면 → 격차는 모델 구조/데이터 활용의 근본 차이이며, 남은 시간엔
   v47(1066.18) 유지가 합리적
