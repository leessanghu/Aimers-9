# 외부 제안 검토 기록 (v42 병목 분석)

## 검토 결과 요약

외부(Codex/논문 기반) 분석에서 4가지 제안이 왔음. 교차검증 결과:

| # | 제안 | 판정 | 근거 |
|---|------|------|------|
| 1 | 고정블렌드 대신 branch selection(fold별 최고조합만 채택) | **이미 해결됨(idea16)** | 4개 멤버 동시 심플렉스 그리드서치가 이미 이걸 연속공간에서 수행함. 이산 branch on/off는 이 탐색의 부분집합. |
| 2 | multires를 보조 branch로, weight 0/0.03/0.05/0.10/0.15 열어두기 | **이미 해결됨(idea16)** | 재검증: mr=0.00→931.40, 0.05→932.12, **0.10→932.53(최적, v42가 이미 사용 중)**, 0.15→932.50 |
| 3 | fold C 수치 불일치(1973.65/2322.41 vs 내 로그 1962.92/2290.92) | **미해결, 출처 확인 필요** | 내 수치는 idea13.log/idea13b_resume.log 타임스탬프 라인에서 직접 인용. 상대 수치의 스크립트/캐시 출처 불명. |
| 4 | LOO prior를 shared-tree head가 아니라 **late-fusion 피처**로 사용 | **유일하게 새로운 방향, 다음 실험 대상** | 지금까지 전부 "shared-tree auxiliary head"(v40/idea12/idea15) 아니면 "완전분리 모델 블렌드"(idea13)만 시도. "prior를 그냥 입력 피처로" 조합은 미시도. 다만 phase93에서 유사 계열(스무딩된 성공률 피처) 정보고갈 결론과 겹칠 위험 있음 — 완전히 같은 피처는 아니라 재검증 필요.

## 다음 실험 (아이디어J, 대기 중)

**Branch D — LOO prior late-fusion feature**

- 투수-시즌 LOO 성공률(head1과 동일한 값)과 투수x손 LOO 성공률(head2와 동일한 값)을
  auxiliary head가 아니라 **일반 입력 피처 2개**로 X에 추가.
- 단일 타겟(y) HGB/CatBoost 분류기 하나를 이 피처 포함 162+2=164 피처로 학습.
- shared-tree 방식(v40/multires)과 직접 비교: 같은 정보(LOO prior)를
  "공유트리 auxiliary head로 정규화 신호로 쓰기" vs "그냥 피처로 주기" 중
  어느 쪽이 fold A/C에서 더 안정적/유효한지 확인.
- 판정 기준: 기존과 동일 — fold A/C 클린폴드 우선, 시드폭 초과 이득만 신뢰,
  fold B는 참고용.

## 진행 중인 실험 (idea20, idea21)

- idea20: HGB 조기종료를 랜덤 검증셋 → 시간순 뒤쪽 8%로 교체.
  fold A 결과: d6 단독 시간순ES가 랜덤ES보다 +49.03(881.33 vs 832.30),
  단 base3(3변종 평균) 안에서는 희석되어 전체블렌드 이득 +1.64.
  fold C/B 진행 중.
- idea21: 상위 단조(monotonic) 피처 15개에 monotonic_cst 제약. 진행 대기 중.
