# 구종 아이디어 실측 결과 보고 + 다음 단계 질문

## 0. 요약

제안해 주신 **train↔Trackman 투구 단위 매칭 → 구종 주변화**를 구현해서 실제 제출했습니다.
**작동했지만 +6.7점**입니다. 사전 신호 측정치(재현상관 0.475, 상한 294점)에 비해 크게 미달했습니다.

동시에 **가중치에 대한 기존 전제가 틀렸다**는 것도 드러났습니다(아래 2절).

---

## 1. 구종 아이디어 실측

### 구현
- 매칭키: `(season, game_month, game_dayofweek, tm_pitcher_id, inning, top_bottom, balls, strikes, outs)`
- 셀 내 `pitch_type_group`이 유일한 경우만 확정 매칭 (sequence alignment는 미사용)
- 테이블 3종을 시즌 누적으로 구축, 각 행은 **season−1까지만 조회**:
  - `ctrl`: (투수, 구종) 성공/시도
  - `mix`: (투수, 카운트, 구종) 빈도
  - `gmix`, `gtype`: 전역 (카운트,구종) 믹스 / 구종별 성공률
- 피처 3개: `pt_pred = Σ_t P(t|투수,카운트)·ctrl(투수,t)`, `pt_dev = pt_pred − prior(투수)`, `pt_n`
- 축소: `K_control=340`(진짜SD 0.0271 기반), `K_mix=80`
- **현재 투구의 구종은 사용하지 않음**, test 행 간 참조 없음, 추론 시 Trackman 파일 불필요

### 사전 검증 (제안하신 채택 기준 전부 통과)
| 항목 | 결과 | 기준 |
|---|---|---|
| 매칭 정밀도 (batter_hand로 교차검증, 매칭키 미사용) | **99.5% / 99.7%** | 99% ✅ |
| 커버리지 | **61.5%** (전체 train 대비) | 50% ✅ |
| 투수×구종 제구력 진짜SD (노이즈 제거) | **0.0271** → 상한 294점 | — |
| 재현상관 (직전시즌 편차→다음시즌) | **r=+0.475** [0.433,0.514] | platoon 0.328보다 높음 ✅ |
| 신호 진폭 `pt_dev` SD | **0.0122** | 성공구간 0.009~0.012 ✅ |
| 구종별 전역 성공률 | fastball .541 / offspeed .513 / breaking .484 | — |

### 폴드 검증 (2024, baseline = v12 정확 재현, CatBoost 3시드)
```
                 hgb     cat3시드   50:50    개별시드
baseline(v12)   741.7    825.1    813.3   [798.5, 820.6, 822.4]
+구종3개        759.2    823.2    821.0   [820.5, 816.1, 805.5]
                +17.5     -1.9     +7.8
```

### 실제 리더보드
```
v12  (구종 없음, 50:50)        963.796
v14a (구종 추가, 50:50)        970.493   d1 = +6.70
v14b (구종 추가, 20:80)        970.546   d2 = +0.05
```

---

## 2. 부수적으로 밝혀진 것: HGB vs CatBoost 전제가 틀렸음

`d2 = +0.05`입니다. **HGB 비중을 0.5→0.2로 줄였는데 점수가 안 움직였습니다.**

그런데 2024 폴드는 `hgb 741.7 vs cat 825.1`로 **84점 차이**라고 말합니다.
즉 **폴드가 말하는 모델 간 격차가 실제 test에는 존재하지 않습니다.**

이건 우리가 그동안 "CatBoost가 압도적이니 비중을 옮겨야 한다"고 믿고 v13에서도 20:80으로 갔던
근거를 무너뜨립니다. (v13은 −11점이었는데, 이제 그 원인에서 가중치는 제외됩니다.)

---

## 3. +6.7에 그친 이유 — 희석이 아니었습니다

처음엔 "구종을 주변화하니 신호가 희석됐다"고 의심했지만, 분산 분해 결과 아닙니다:

```
pt_dev 전체 SD       = 0.01217
  투수간(between) SD = 0.00681   (분산의 31.4%)  <- prior와 중복되는 성분
  투수내(within)  SD = 0.01008   (분산의 68.6%)  <- 카운트 변화가 만드는 순수 신규 신호
```

**순수 신규 신호가 0.0101로 전체의 68.6%**입니다. platoon(0.0119)과 거의 같은 진폭입니다.

그런데 실제 이득은:
- platoon: 진폭 0.0119 → **+14점**
- 구종: 진폭 0.0101(순수분) → **+6.7점**

**진폭은 비슷한데 이득이 절반입니다.** 재현상관은 오히려 구종(0.475)이 platoon(0.328)보다 높은데도요.
저희가 쓰던 "진폭 + 재현상관" 기준으로는 이 차이를 설명하지 못합니다.

가능한 가설(검증 못 함):
- (a) 구종 신호가 이미 모델 안의 다른 피처(카운트, 투수 실력, 커리어 pitchmix rate)와 공선성이 큼
- (b) `P(t|투수,카운트)` 추정이 부정확해서 주변화 단계에서 오차가 들어감
- (c) 2025 시즌에 구종 구성이 변한 투수들에서 테이블이 어긋남

---

## 4. 전체 제출 이력 (실측, 순서대로)

| 버전 | 변경 내용 | 점수 | 델타 |
|---|---|---|---|
| v4 | 58 base + in-season(5), RF+HGB | 925.908 | — |
| v7b | + platoon(2), HGB 단독 | 939.681 | +13.8 |
| v7c | + inning(2) | 948.970 | +9.3 |
| v9 | v7c − batter_asof(4) | 922.415 | **−26.6** (로컬 ablation은 +33이었음) |
| v8 | v9 + 칼만 + CatBoost | 939.875 | +17.5 (v9 대비) |
| v10 | v7c + 칼만교체 + CatBoost | 950.813 | +1.8 |
| v11 | v10 + 교차항(14) | 956.493 | +5.7 |
| v12 | in-season 원본 + 교차항 + CatBoost 50:50 | 963.796 | +7.3 (칼만 폐기) |
| v13 | + 작년피처(7) + K재조정 + 3시드 + 20:80 | 952.826 | **−11.0** |
| **v14a** | **v12 + 구종(3), 50:50** | **970.493** | **+6.7** |
| v14b | v14a에서 20:80으로만 변경 | 970.546 | +0.05 |

---

## 5. 그동안 측정해서 기각한 것들 (재제안 불필요)

- **조건부 분할**: 투수×볼카운트(재현상관 0.121)−12.4 / 타자×투수손(0.257)−3.5 / 투수×월(0.079)−31.3 /
  투수×상대팀(0.054)−4.8 / 투수×주자유무·2루주자·아웃카운트(진짜SD **0.000**) / 투수×점수차·레버리지
- **난수 대조군**: 난수 4개 추가 = −0.9. 위 실패 피처들은 −6~−31 → **약한 구조적 신호는 난수보다 해로움**
- **Trackman 물리량**: 시즌평균 20개 −14.6~−28.1 / 구속변화 r=−0.052(CI에 0) /
  **릴리스포인트 산포** r=+0.060(CI에 0) / 익스텐션 산포는 제구 수준과만 상관(변화와는 무상관)
- **in-season 확장**: 타자 in-season −6.4 / 피치믹스 in-season −6.5 / 둘 다 −7.0
- **칼만 필터**(상태공간 실력추정): in-season 원본이 더 나음(956.5 vs 963.8)
- **시대 보정**(리그수준 상대화): 다음시즌 예측 R² 0.2705→0.2778, 두 변수 상관 0.969 → 무의미
- **모델 용량**: leaves 63/127, depth 8 → −106~−143. 축소도 −5~−43. **현재가 양방향 최적점**
- **엔티티 임베딩 NN**: 임베딩이 과적합 원인(등장 BSS 0.0044 < 미등장 0.0057), 순수 MLP가 나음.
  GBM과 상관 0.75라 블렌드 이득은 있으나 GBM 앙상블 도입 후 +5.4로 축소
- **작년 한 시즌 피처**: 독립 측정은 좋았으나(다음시즌 R² 0.2821→0.3051) v13에 묶여서 실측 불명

---

## 6. 방법론적 제약 (중요)

- 2024 폴드의 **시드 노이즈 SD ≈ 11점** (동일 설정, 시드만 바꿔 804.3~826.1).
- 최근 실제 개선폭은 +2~+9점 → **측정 대상이 측정 오차보다 작음.**
- 로컬이 부호를 틀린 실측 사례: batter_asof 제거(+33→−26.6), workload/form(+17→−4.2),
  그리고 이번 모델 가중치(폴드 84점 차 → 실제 0.05점 차).
- → 현재는 "폴드와 무관한 독립 측정 / 여러 조건 일관성 / 이론적 보장" 중 하나가 없으면 채택 불가.
- **로컬→실제 환산비**는 대략 1.15~1.20배 (v12: 로컬 805.8 → 실제 963.8).

---

## 7. 상한 분석 (2024 폴드, 정답 컨닝)

| | 로컬 |
|---|---|
| 상수(리그평균) | 0 |
| 우리 모델 | ~821 |
| **투수의 2024 실제 성공률(축소판)을 안다고 가정** | **946.5** |
| **위 + 볼카운트 주효과** | **981.0** |

- 투수 실력 진짜 개인차 SD=0.0555 → BSS 상한 0.0123 = **1235점**
- 실제 환산 시 "투수 완벽 + 볼카운트" ≈ **1177점**. 1100점 = 로컬 ~920점.
- 현재 970.5. **1위는 약 1200점.**

---

## 8. 참고: 지금까지 제일 크게 오른 지점 — in-season (+114점)

여기 정리한 실패/미미한 결과들과 대비시키려고 남깁니다. **811.9 → 925.9로 단일 최대 승자**였고,
그 뒤로 나온 모든 성공(플래툰 +14, 이닝 +9, 구종 +6.7)보다도 압도적으로 컸습니다.
**다음 아이디어를 낼 때 이 방향을 최우선으로 다시 훑어봐 주셨으면 합니다.**

### 어떻게 찾았는가 — 재현 가능한 사고 절차

**1) 데이터 구조에서 "숨겨진 정수"를 봤다.**
`asof_pitcher_success_rate`는 소수인데 `asof_pitcher_n`(투구 수)이 같이 있다.
`round(rate × n)`으로 **누적 성공 횟수를 정수로 정확히 복원**할 수 있다.
소수 하나로는 못 하던 산술(뺄셈)을, 정수로 바꾸면 할 수 있게 된다는 게 핵심 트릭이었다.

**2) "누적"이라는 단어를 의심했다.**
`asof_*` 컬럼은 전부 **커리어 누적**이다. 그런데 `season` 컬럼이 따로 존재한다는 건
주최측도 시즌 단위 구분이 의미 있다고 본다는 신호다. "이 투수의 커리어 통산 실력"과
"이 투수의 **올해** 실력"은 다를 수 있고, 후자가 예측에 더 맞을 가능성이 크다고 판단했다.
특히 시즌별 리그 평균 성공률이 0.543(2019) → 0.486(2024)로 **꾸준히 하락**하는 걸 확인하고
드리프트가 실재한다고 확신했다 (이 하락폭이 나중에 투수 개인차 SD 0.0555와 맞먹는
크기라는 것도 별도로 확인됨 — §7 참고).

**3) 그 정수 트릭을 시간축에 적용했다.**
(투수, 시즌)별로 **그 시즌 마지막 행의 누적값**을 모으면 "직전 시즌 종료 시점의 누적
성공 횟수" 테이블을 만들 수 있다. 그러면:
```
이번 시즌 성공 횟수 = 현재 누적 - 직전 시즌 종료 시점 누적
```
새 데이터를 전혀 안 쓰고, **주최측이 이미 준 두 숫자의 차**로 아무도 안 준 "시즌 한정" 정보를
복원한 것이다. leakage 안전성도 자동으로 보장된다(그 투수의 과거 시즌만 참조, 같은 시즌
다른 행/test 행 참조 없음).

### 왜 이게 유독 컸는가 (사후 분석)

이번 세션에서 반복 확인한 원칙과 정확히 반대 극단에 있다:
> "그 행 컬럼만으로 계산 가능한 건 정보가 아니라 트리가 이미 할 수 있는 것"
> (workload/form −4.2, batter_asof 제거 −26.6 등이 이 경계에서 실패)

in-season은 그 행 컬럼만으로는 절대 계산이 안 된다 — **그 투수의 과거 시즌 행들을
따로 집계**해야만 나온다. 그리고 드리프트 크기 자체가 컸다(리그 평균 하락폭이
투수 개인차와 맞먹음). 플래툰(r=0.328)이나 구종(r=0.475)도 재현상관은 강했지만
어차피 원래 있던 `asof_pitcher_success_rate`의 조건부 분해였던 반면,
**in-season은 아예 다른 시간축의 정보**였다는 게 결정적 차이로 보인다.

### 일반화된 탐색 지침 (다음 아이디어를 낼 때 적용해 주시면 좋겠습니다)

1. **"n과 rate가 함께 있는 컬럼 쌍"을 찾아 정수 복원이 가능한지 확인** — 이미 `asof_pitcher_*`,
   `asof_batter_*`, `asof_pitcher_pitchmix_*`, `prev1/3/5_game_*`에 적용했음(뒤의 셋은
   시도했으나 실패 — §5 참고. 성공한 건 투수 success/ball/reverse뿐이었음. 왜 나머지는
   실패했는지도 같이 봐주시면 좋겠습니다)
2. **"누적/평균"이라는 단어가 붙은 컬럼은 전부 의심** — 시간 분해가 안 됐는지, 분해하면
   드리프트나 최근성이 드러나는지 체크
3. **분해로 얻은 신호의 "출처가 완전히 다른 시간축인지"를 확인** — 기존 컬럼의 조건부 분해
   (플래툰/이닝/구종 계열)보다 **새로운 시간 스냅샷**이 훨씬 크게 작동했다

---

## 9. 질문

1. **구종 축을 더 파는 게 맞을까요?**
   순수 신규 신호 진폭 0.0101에 재현상관 0.475인데 +6.7만 나왔습니다.
   3절의 가설 (a)공선성 / (b)`P(t|x)` 추정오차 / (c)2025 구종구성 변화 중
   무엇을 먼저 검증해야 할지, 그리고 개선한다면 어디를 건드려야 할지 의견 부탁드립니다.
   (예: sequence alignment로 커버리지 61.5%→90%+ 올리기? `P(t|x)`에 타자손·이닝 추가?
    구종을 4그룹이 아니라 세부 구종으로? 구종별 제구력을 카운트와 교차?)

2. **"진폭 + 재현상관"이 이득을 예측하지 못했습니다.**
   platoon(진폭 0.0119, r=0.328) → +14 vs 구종(0.0101, r=0.475) → +6.7.
   피처의 실제 가치를 사전에 더 잘 예측할 지표가 있을까요?
   (기존 피처들과의 부분상관/증분 R² 같은 걸 봐야 할까요?)

3. **HGB와 CatBoost가 실제로는 동등**한데 폴드는 84점 차이를 보입니다.
   이게 왜 생기는지, 그리고 앙상블 설계를 어떻게 바꿔야 할지 의견 부탁드립니다.
   (XGBoost/LightGBM 추가는 폴드 기준으로는 CatBoost보다 약했으나, 폴드를 못 믿는 상황입니다.)

4. **970 → 1100** 에 남은 130점을 어디서 찾아야 할까요?
   7절 상한 분석대로면 "투수 현재 실력 추정 정밀도"에 대부분 있는데,
   저희가 시도한 것들(칼만, 작년피처, K 재조정)은 전부 실패했거나 미미했습니다.
   **8절의 in-season 사례(단일 최대 승자, +114)를 최우선 참고 삼아 주십시오.**
   같은 "n·rate 쌍 정수복원 + 시간축 분해" 패턴을 아직 안 써본 컬럼이나 조합이 있을까요?
   (예: `asof_batter_n`+`asof_batter_success_rate`로 타자 쪽 시즌분해는 시도해 실패(-6.4)했는데,
   그건 왜 실패했고 투수 쪽과 뭐가 다른지도 궁금합니다.)
